#!/usr/bin/env sh

# Global yabai config
yabai -m config                                 \
    external_bar                 off:40:0       \
    menubar_opacity              1.0            \
    mouse_follows_focus          off            \
    focus_follows_mouse          off            \
    display_arrangement_order    default        \
    window_origin_display        default        \
    window_placement             second_child   \
    window_insertion_point       focused        \
    window_zoom_persist          on             \
    window_shadow                on             \
    skip_window_focus_animation  off            \
    window_animation_duration    0.0            \
    window_animation_easing      ease_out_circ  \
    window_opacity_duration      0.0            \
    active_window_opacity        1.0            \
    normal_window_opacity        0.90           \
    window_opacity               off            \
    insert_feedback_color        0xffd75f5f     \
    split_ratio                  0.50           \
    split_type                   auto           \
    auto_balance                 on             \
    top_padding                  12             \
    bottom_padding               12             \
    left_padding                 12             \
    right_padding                12             \
    window_gap                   06             \
    layout                       bsp            \
    mouse_modifier               fn             \
    mouse_action1                move           \
    mouse_action2                resize         \
    mouse_drop_action            swap

# Default: manage=off for everything (float like normal macOS)
yabai -m rule --add manage=off

# Only tile specific apps on the rightmost display (display index 2)
# Tile Google Chrome on display 2 (rightmost)
yabai -m rule --add app="^Google Chrome$" display=2 manage=on
# Also tile Chromium if used (CloakBrowser)
yabai -m rule --add app="^Chromium$" display=2 manage=on
# Optionally tile Firefox, Safari if desired
# yabai -m rule --add app="^Firefox$" display=2 manage=on
# yabai -m rule --add app="^Safari$" display=2 manage=on

# Float common utility apps everywhere (even on right screen)
yabai -m rule --add app="^System Settings$" manage=off
yabai -m rule --add app="^System Information$" manage=off
yabai -m rule --add app="^Archive Utility$" manage=off
yabai -m rule --add app="^Tailscale$" manage=off
yabai -m rule --add app="^1Password$" manage=off
yabai -m rule --add app="^QuickTime Player$" manage=off
yabai -m rule --add app="^Finder$" title="(Copy|Move|Trash|Delete|Info|Preferences)" manage=off
yabai -m rule --add subrole="^AXDialog$" manage=off
yabai -m rule --add subrole="^AXSystemDialog$" manage=off
yabai -m rule --add role="^AXPopover$" manage=off

echo "yabai configuration loaded.."
